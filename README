打包方式: pyinstaller --onefile --add-data "web;web" main.py // --noconsole 

============================================================================

問題紀錄:

1. 有時候證券網頁登入或打開如果有問題，可能是插件沒啟動，直接重開機看看

2. 如果是自動化瀏覽器打不開，試著打開任務管理員，找到chrome.exe把它關掉

3. 自動化瀏覽器有設定給Selinium專用的Chrome Profile，設定方法可以看crawl_utils裡面的init_driver的註解


